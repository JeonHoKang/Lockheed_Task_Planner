"Take a look at the example policies that I have prepared. Based on the user input that will come at the end, 
using the same functions in example policies generate a code that reflects user's instruction to nodes in htn 
You should just write single function that reflects user's input and nothing else. make sure that the function name is not
duplicate of the existing function in example procedures"